services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB=db
      - POSTGRES_PASSWORD=pwd
  server:
    build: .
    ports:
      - 3070:8080
    env_file:
      - .env
    depends_on:
      - db
